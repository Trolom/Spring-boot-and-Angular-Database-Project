import { TrainingCertificate } from './training-certificate';

describe('TrainingCertificate', () => {
  it('should create an instance', () => {
    expect(new TrainingCertificate()).toBeTruthy();
  });
});
